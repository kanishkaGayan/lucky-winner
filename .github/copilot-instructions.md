# Workspace Instructions

- Keep the Lucky Winner app focused on the two requested routes: `/register` and `/winner`.
- Preserve the dark gold/neon visual style and the automatic winner selection flow.
- Keep Firebase access in `src/firebase/config.js` and document setup in `README.md`.
